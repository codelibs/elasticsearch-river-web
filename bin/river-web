#!/bin/bash

cd `dirname $0`
cd ..
BASE_DIR=`pwd`
CONFIG_DIR=$BASE_DIR/config
LIB_DIR=$BASE_DIR/lib

CP_PATH=$CONFIG_DIR
for JAR_FILE in `ls $LIB_DIR/*.jar 2>/dev/null` ; do
  CP_PATH="$CP_PATH;$JAR_FILE"
done

java -cp $CP_PATH org.codelibs.elasticsearch.web.RiverWeb $@

